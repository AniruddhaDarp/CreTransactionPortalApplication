export function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', maxWidth: 640, margin: '4rem auto' }}>
      <h1>CRE Transaction Portal</h1>
      <p>
        A neutral, per-deal workspace for executing a commercial property purchase. Scaffold only —
        authentication and the deal workspace arrive in later modules.
      </p>
    </main>
  );
}
